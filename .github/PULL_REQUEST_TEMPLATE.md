## What this does

Brief description of the change and the issue it closes (if any).

## Checklist

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] New detectors/validators have unit tests (and eval fixtures where it fits)
- [ ] Core stays dependency-free (heavy runtimes go behind an interface)
- [ ] Docs updated if behavior or the public API changed
